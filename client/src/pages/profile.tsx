interface ProfileProps { }

export const Profile: React.FC<ProfileProps> = ({ }) => {
  // get the user data
  //  three main components, fetch data here based on userid
  return <div>Profile Page</div>
}
